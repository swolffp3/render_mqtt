# Allow anonymous access for testing
allow_anonymous true

# Listen on all interfaces (0.0.0.0) to avoid network binding issues
listener 1883 0.0.0.0

# Enable WebSockets (optional)
listener 9001 0.0.0.0
protocol websockets

